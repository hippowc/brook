# Docker —— 容器引擎：官方脚本 get.docker.com 国内不可达，brook 内置国内仓库直装
# macOS 无 apt/dnf/yum：请用 Docker Desktop（brew install --cask docker）
DESC="容器引擎（brook 内置国内仓库直装，需 root/sudo；macOS 用 Docker Desktop）"
CATEGORY="installer"
INSTALL_LOCAL="docker-install.sh"
NEED_ROOT=1
BINARIES="docker"
