# git —— 版本控制：系统自带（apt/brew/CLT），brook 不代装，只提供配置实践
DESC="版本控制（系统自带，brook 管配置）"
CATEGORY="binary"
EXTERNAL_INSTALL=1
INSTALL_HINT="Linux: sudo apt-get install -y git；macOS: xcode-select --install（CLT 自带）"
BINARIES="git"
